# Required: endpoint that receives POST { email, source, metadata, submittedAt }
VITE_WAITLIST_ENDPOINT=https://your-api.com/waitlist

# Optional: API key if your endpoint expects Authorization Bearer token
VITE_WAITLIST_API_KEY=
